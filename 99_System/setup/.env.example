# Template variabili ambiente — copia in `.env` (gitignored)
# Le credenziali reali stanno in 1Password — chiedi a {{ ADMIN_NAME }}

USER_NAME=
USER_EMAIL=

# Anthropic — usato dal `claude` CLI (auto dopo `claude login`)
# ANTHROPIC_API_KEY=

# MCP servers (riempire solo se servono)
# FATHOM_API_KEY=
# NOTION_API_KEY=
# SLACK_BOT_TOKEN=
