# 99_System/setup

Script e documentazione per:
1. Onboarding nuovi soci (`onboarding.sh` o link al pubblico)
2. Configurazione MCP (`.mcp.json.example`)
3. Variabili ambiente (`.env.example`)

In produzione, il setup script viene scaricato dal repo pubblico
{{ SETUP_REPO_URL }}.
